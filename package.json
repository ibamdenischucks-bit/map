{
  "name": "waps-dashboard",
  "version": "1.0.0",
  "description": "Social platform for profile sharing and XP system",
  "scripts": {
    "build": "echo 'No build required'"
  },
  "engines": {
    "node": "18"
  }
}